(()=>{const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)],MEM='una-vita-real-memories-v1',PRO='una-vita-real-profile-v1',DB='una-vita-media-v1';let scenes=[],idx=0,timer=null,playing=true,urls=[],audioOn=true,ctx=null,gain=null,oscA=null,oscB=null;const getMem=()=>{try{return JSON.parse(localStorage.getItem(MEM)||'[]')}catch{return[]}},getPro=()=>{try{return JSON.parse(localStorage.getItem(PRO)||'{}')}catch{return{}}};function period(y,b){if(!b)return'Un ricordo';const a=y-b;if(a<=11)return'Infanzia';if(a<=19)return'Adolescenza';if(a<=30)return'Giovinezza';if(a<=55)return'Età adulta';if(a<=70)return'Maturità';return'Vecchiaia'}function dbAll(){return new Promise((res,rej)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('media'))r.result.createObjectStore('media',{keyPath:'id'})};r.onsuccess=()=>{const t=r.result.transaction('media','readonly'),g=t.objectStore('media').getAll();g.onsuccess=()=>res(g.result||[]);g.onerror=()=>rej(g.error)};r.onerror=()=>rej(r.error)})}async function build(){urls.forEach(URL.revokeObjectURL);urls=[];const p=getPro(),mem=getMem(),media=await dbAll(),byYear={};mem.forEach(m=>(byYear[m.year]??={year:m.year,mem:[],media:[]}).mem.push(m));media.forEach(m=>(byYear[m.year]??={year:m.year,mem:[],media:[]}).media.push(m));scenes=Object.values(byYear).sort((a,b)=>a.year-b.year).map(s=>{const age=p.birth?s.year-p.birth:null,where=s.mem.find(x=>x.place)?.place||'',texts=s.mem.map(x=>x.text),title=age===0?'L’inizio':where||period(s.year,p.birth);let narration=texts.length?texts.join(' '):`Un frammento del ${s.year} è rimasto nel tuo archivio.`;if(age!=null&&age>=0)narration=`Avevi ${age} ${age===1?'anno':'anni'}. ${narration}`;return{...s,period:period(s.year,p.birth),title,narration}});return scenes}function showScreen(name){qa('.screen').forEach(x=>x.classList.remove('active'));q(name).classList.add('active')}function initAudio(){if(ctx)return;const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return;ctx=new AC();gain=ctx.createGain();gain.gain.value=.035;gain.connect(ctx.destination);oscA=ctx.createOscillator();oscB=ctx.createOscillator();oscA.type='sine';oscB.type='triangle';oscA.frequency.value=174;oscB.frequency.value=261.6;const g1=ctx.createGain(),g2=ctx.createGain();g1.gain.value=.6;g2.gain.value=.22;oscA.connect(g1).connect(gain);oscB.connect(g2).connect(gain);oscA.start();oscB.start()}function setMood(p){if(!ctx||!oscA||!oscB)return;const map={Infanzia:[196,293.7],Adolescenza:[164.8,246.9],Giovinezza:[174.6,261.6],'Età adulta':[146.8,220],Maturità:[130.8,196],Vecchiaia:[123.5,185]};const [a,b]=map[p]||[174.6,261.6];oscA.frequency.cancelScheduledValues(ctx.currentTime);oscB.frequency.cancelScheduledValues(ctx.currentTime);oscA.frequency.linearRampToValueAtTime(a,ctx.currentTime+1.3);oscB.frequency.linearRampToValueAtTime(b,ctx.currentTime+1.3)}function speak(text){if(!audioOn||!('speechSynthesis'in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='it-IT';u.rate=.92;u.pitch=.95;u.volume=.95;const voices=speechSynthesis.getVoices();u.voice=voices.find(v=>v.lang?.toLowerCase().startsWith('it'))||null;speechSynthesis.speak(u)}async function start(){await build();if(!scenes.length){alert('Aggiungi almeno un ricordo o una foto prima di creare il film della tua vita.');return}idx=0;playing=true;showScreen('#cinema-screen');initAudio();if(ctx?.state==='suspended')await ctx.resume();if(gain)gain.gain.value=audioOn?.035:0;render(true);schedule()}async function render(withVoice=true){clearTimeout(timer);const s=scenes[idx];q('#cinema-period').textContent=`${s.period} · ${s.year}`;q('#cinema-title').textContent=s.title;q('#cinema-narration').textContent=s.narration;q('#cinema-progress-label').textContent=`${idx+1} / ${scenes.length}`;q('#cinema-progress').style.width=`${((idx+1)/scenes.length)*100}%`;const box=q('#cinema-media'),bg=q('#cinema-bg');box.innerHTML='';bg.style.backgroundImage='';if(s.media.length){const m=s.media[0],u=URL.createObjectURL(m.blob);urls.push(u);if((m.type||'').startsWith('video/')){const v=document.createElement('video');v.src=u;v.autoplay=true;v.muted=true;v.loop=true;v.playsInline=true;box.appendChild(v);bg.style.backgroundImage='linear-gradient(#101820,#05080c)'}else{const im=document.createElement('img');im.src=u;box.appendChild(im);bg.style.backgroundImage=`url("${u}")`}}else{bg.style.backgroundImage='radial-gradient(circle at 30% 30%,#263748,#080d12 65%)'}q('#cinema-play').textContent=playing?'❚❚':'▶';q('#cinema-sound').textContent=audioOn?'◉':'○';setMood(s.period);if(withVoice&&playing)speak(`${s.period}, ${s.year}. ${s.narration}`)}function schedule(){clearTimeout(timer);if(playing)timer=setTimeout(()=>{if(idx<scenes.length-1){idx++;render(true);schedule()}else{playing=false;q('#cinema-play').textContent='▶';window.speechSynthesis?.cancel()}},9000)}q('#play-life').onclick=start;q('#cinema-next').onclick=()=>{idx=Math.min(scenes.length-1,idx+1);render(true);schedule()};q('#cinema-prev').onclick=()=>{idx=Math.max(0,idx-1);render(true);schedule()};q('#cinema-play').onclick=()=>{playing=!playing;q('#cinema-play').textContent=playing?'❚❚':'▶';if(!playing)window.speechSynthesis?.cancel();else speak(`${scenes[idx].period}, ${scenes[idx].year}. ${scenes[idx].narration}`);schedule()};q('#cinema-close').onclick=()=>{clearTimeout(timer);window.speechSynthesis?.cancel();if(gain)gain.gain.value=0;showScreen('#real-screen')};q('#cinema-sound').onclick=async()=>{audioOn=!audioOn;initAudio();if(ctx?.state==='suspended')await ctx.resume();if(gain)gain.gain.value=audioOn?.035:0;q('#cinema-sound').textContent=audioOn?'◉':'○';if(!audioOn)window.speechSynthesis?.cancel();else if(playing)speak(`${scenes[idx].period}, ${scenes[idx].year}. ${scenes[idx].narration}`)};})();